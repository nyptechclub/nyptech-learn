export default function Main({
    children,
  }: {
    children: React.ReactNode
  }) {
    return (
        <div>
        {children}
        </div>
    );
}

//I use this for easy copy, feel free to remove and add to docs or smth --sven